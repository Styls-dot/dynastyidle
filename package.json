{
  "name": "dynasty-game",
  "version": "1.0.0",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "npm ci --prefix server && npm ci --prefix client && npm --prefix client run build",
    "start": "node server/index.js"
  }
}
